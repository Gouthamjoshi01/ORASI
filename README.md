# 🌸 Orasi Auditing Platform v1.0 ⚔️
An ultra-modern, high-concurrency network reconnaissance utility and live vulnerability mapping tool. Engineered to bridge the gap between dual-engine socket scanning and enterprise sub-process orchestration.

## 🛠️ System Architecture
Unlike traditional sequential tools that check ports one by one, Orasi features an advanced **Dual-Core Execution Engine**:
* **High-Speed Threaded Fallback:** Utilizes Python's native `concurrent.futures` to manage thread-locked connection workers, slicing scan latencies across multi-tier networks from minutes to seconds.
* **Pass-Through Orchestration Engine:** Intercepts system process pipelines to inject advanced firewall evasion architectures (decoys, custom data padding) while maintaining strict terminal aesthetic branding.
* **Kyoto Split-Pane Assessment Drawer:** Leverages modular dropdown text nodes to isolate verbose service banner script dumps (such as SSH host keys or FTP system properties) without throwing horizontal display grids out of column alignment.

## 🚀 Live Visuals & Interface Profile
> Insert your custom visual confirmation screenshot showing the vibrant pink sakura canopy, the curved katana, and a live vulnerability mapping output loop block here.

## 📊 Feature Metrics & Capabilities
* **Hardened Resiliency Layer:** Configured with a 2.5–3.0 second socket timeout parameter optimized explicitly to traverse latent OpenVPN routing interfaces or unstable CTF platform environments.
* **Passive CVE Correlation Engine:** Maps discovered raw banner keywords, application structures, and operational states against a local vulnerability array, immediately surface security defects directly inside the terminal layout.
* **Automated Parameter Parser:** Features an intuitive built-in `-h / --help` CLI dashboard that auto-compiles configuration flags seamlessly.

## 💻 Operational Execution Examples
Ensure you execute infrastructure scanning tasks with elevated context (`sudo`) when calling low-level raw network socket adjustments or stealth SYN mechanisms:

```bash
# Execute a fast, stealth OS discovery scan with strict thread timing metrics
sudo python3 scanner.py -sS -O --min-rate 500 10.49.169.61

# Trigger a complex firewall evasion sweep utilizing random decoy targets
sudo python3 scanner.py -D RND:5 --data-length 25 10.49.169.61
